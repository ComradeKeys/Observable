<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                tools:context=".MainActivity"
                android:background="#000000">

    <TextView
        android:text="@string/MainTitle"
        android:layout_width="wrap_content"
        android:textSize="50sp"
        android:layout_height="wrap_content"
        android:id="@+id/Observable"
        android:textColor="#FFFFFF"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"/>

    <Button
        android:layout_width="200sp"
        android:layout_height="50sp"
        android:text="@string/Planetarium"
        android:id="@+id/planetariumButton"
        android:onClick="onClickPlanetarium"
        android:layout_marginTop="73dp"
        android:layout_below="@+id/Observable"
        android:layout_centerHorizontal="true"/>

    <Button
        android:layout_width="200sp"
        android:layout_height="50sp"
        android:text="Options"
        android:id="@+id/optionsButton"
        android:onClick="onClickOptions"
        android:layout_below="@+id/planetariumButton"
        android:layout_alignStart="@+id/planetariumButton"/>

    <Button
        android:layout_width="200sp"
        android:layout_height="50sp"
        android:text="About"
        android:id="@+id/aboutButton"
        android:onClick="onClickAbout"
        android:layout_below="@+id/optionsButton"
        android:layout_alignStart="@+id/optionsButton"/>
    <Button
        android:layout_width="200sp"
        android:layout_height="50sp"
        android:text="Exit"
        android:id="@+id/exitButton"
        android:onClick="onClickExit"
        android:layout_below="@+id/aboutButton"
        android:layout_alignStart="@+id/aboutButton"/>

</RelativeLayout>
